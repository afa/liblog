<div><%= h @author.first_name -%></div>
<div><%= h @author.last_name -%></div>
<div>
 <% @author.books.each do |book| %>
  <div><%= link_to h(book.name), lib_book_path(book) %></div>
 <% end %>
</div>

