<table>
<tr><th>Название</th><th>Авторы</th><th>ДД</th></tr>
 <%# @page.books.each do |book| %>
 <% @books.each do |book| %>
  <tr>
   <td><%= book.name -%></td>
   <td><%= book.authors.map(&:name).join ', ' -%></td>
   <td><%= link_to "show", lib_book_path(book) -%>
   <%= link_to "edit", edit_lib_book_path(book) -%></td>
  </tr>
 <% end %>
</table>
<%= will_paginate @books %>
<ul>
<%# @page.paginator.window(:inner => 2, :outer => 1) do |page, path, classes| %>
    <%# content_tag_for :li, page, :class => classes.join(" ") do %>
      <%#= link_to_if path, page.number, path %>
    <%# end %>
  <%# end %>
</ul>
